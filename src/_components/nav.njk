<div class="core_navigation headroom">
  <div class="container container-xl">
    <nav class="core_navigation__nav">
      <div class="row">
        <div class="column large-4">
            <div class="core_navigation__brand">
              <a href="{{ '/' | url }}">{{ metadata.title }}</a>
            </div>
        </div>
        <div class="column large-8">
            <div class="core_navigation__links">
              {% set navPages = collections.all | eleventyNavigation %}
              <div class="core_navigation__links_container">
              {%- for entry in navPages %}
                <div class="core_navigation__links_item"  data-text="{{ entry.title }}">
                  {% if entry.url == page.url %}
                    <div class="button button-outline-dark" {% if entry.url == page.url %} data-current="current item"{% endif %}>{{ entry.title }}</div>
                  {% else %}
                    <a class="button button-outline-dark" href="{{ entry.url | url }}"{% if entry.url == page.url %} data-current="current item"{% endif %}>{{ entry.title }}</a>
                  {% endif %}
                </div>
              {%- endfor %}
              <label class="switch">
                <input type="checkbox">
                <span class="slider round themeingbutton"></span>
              </label>
            </div>
          </div>
        </div>
      </div>
    </nav>
  </div>
</div>
